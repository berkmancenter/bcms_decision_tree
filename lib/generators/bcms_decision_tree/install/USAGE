Description:
    Installs the bcms_decision_tree module.

Example:
    rails generate bcms_decision_tree:install

    This will:
		1. Copy any migrations from the gem into the project.
		2. Add the routes to the config/routes.rb

